interface:
  display_name: "Skill Governor"
  short_description: "Route, deduplicate, fill, and evolve skill libraries"
  default_prompt: "Use $skill-governor to assess capability fit, fill real gaps, and avoid duplicate skills."
