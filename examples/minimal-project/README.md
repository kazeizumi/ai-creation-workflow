# Minimal project example

This example demonstrates structure only. `Picture_01.placeholder.txt` is not
media and the workflow does not generate useful output. Both included manifests
are intended for `--dry-run` validation until you replace the placeholders with
your own approved workflow, references, prompt and runtime identifiers.
